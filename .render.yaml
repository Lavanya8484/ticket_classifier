services:
  - type: web
    name: ticket-classifier
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app.py"
    region: oregon
    plan: free
    runtime: python
    pythonVersion: 3.10
